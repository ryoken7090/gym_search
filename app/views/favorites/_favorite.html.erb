<% if user_signed_in? %>
  <p class="text-right">
    <% if favorite.present? %>
      <%= link_to 'お気に入り解除', favorite_path(id: favorite.id), method: :delete, class: 'btn btn-danger', remote: true %>
    <% else %>
      <%= link_to 'お気に入りする', favorites_path(facility_id: facility.id), method: :post, class: 'btn btn-primary', remote: true %>
    <% end %>
  </p>
<% end %>
