<% if @review.errors.any? %>
  <div id="error_explanation">
    <%= render 'layouts/flash' %>
    <h5><%= @review.errors.count %>件のエラーがあります。</h5>
    <ul>
      <% @review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
